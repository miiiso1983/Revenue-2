# ⚡ دليل النشر السريع على Cloudways

## 🎯 الخطوات المطلوبة (5-10 دقائق)

---

## 📋 المتطلبات الأساسية

✅ حساب Cloudways نشط  
✅ تطبيق PHP على Cloudways  
✅ URL: https://phpstack-1510634-6068149.cloudwaysapps.com  

---

## 🚀 خطوات النشر

### 1️⃣ الحصول على بيانات SSH

1. افتح لوحة تحكم Cloudways
2. اذهب إلى **Servers** → اختر السيرفر الخاص بك
3. اذهب إلى **Master Credentials**
4. انسخ:
   - **SSH Username** (مثل: `master_abcdefgh`)
   - **SSH Password** أو استخدم **SSH Key**
   - **Server IP** (مثل: `123.45.67.89`)

### 2️⃣ الاتصال بالسيرفر

```bash
# استبدل القيم بالقيم الخاصة بك
ssh master_username@server_ip
```

### 3️⃣ الحصول على بيانات قاعدة البيانات

في لوحة تحكم Cloudways:
1. اذهب إلى **Applications** → اختر تطبيقك
2. اذهب إلى **Access Details**
3. انسخ:
   - **Database Name** (مثل: `abcdefgh_revenue`)
   - **Database Username** (مثل: `abcdefgh_user`)
   - **Database Password**

### 4️⃣ الانتقال لمجلد التطبيق

```bash
# استبدل app_name باسم تطبيقك
cd /home/master_username/applications/app_name/public_html
```

### 5️⃣ حذف الملفات الافتراضية

```bash
# احذف الملفات الافتراضية
rm -rf *
rm -rf .[!.]*
```

### 6️⃣ استنساخ المشروع من GitHub

```bash
git clone https://github.com/miiiso1983/Revenue-2.git .
```

### 7️⃣ تشغيل سكريبت الإعداد الأولي

```bash
# تشغيل السكريبت
bash initial-setup-cloudways.sh
```

**السكريبت سيطلب منك:**
1. تعديل ملف `.env` بإضافة بيانات قاعدة البيانات
2. اختيار ما إذا كنت تريد إضافة بيانات تجريبية

### 8️⃣ تعديل ملف .env

```bash
nano .env
```

**عدّل هذه الأسطر:**
```env
DB_DATABASE=your_database_name_from_step_3
DB_USERNAME=your_database_username_from_step_3
DB_PASSWORD=your_database_password_from_step_3
```

**احفظ الملف:**
- اضغط `Ctrl + X`
- اضغط `Y`
- اضغط `Enter`

### 9️⃣ إعداد Webroot (مهم جداً!)

في لوحة تحكم Cloudways:
1. اذهب إلى **Application Settings**
2. ابحث عن **Webroot**
3. غيّره من `public_html` إلى `public_html/public`
4. احفظ التغييرات

### 🔟 تفعيل SSL

في لوحة تحكم Cloudways:
1. اذهب إلى **SSL Certificate**
2. اختر **Let's Encrypt**
3. أدخل البريد الإلكتروني
4. اضغط **Install Certificate**

---

## ✅ التحقق من النشر

افتح المتصفح واذهب إلى:
```
https://phpstack-1510634-6068149.cloudwaysapps.com
```

يجب أن ترى صفحة تسجيل الدخول! 🎉

---

## 🔐 بيانات الدخول الافتراضية

**حساب المدير:**
- البريد: `admin@example.com`
- كلمة المرور: `password`

**حساب الضيف:**
- البريد: `guest@example.com`
- كلمة المرور: `password`

⚠️ **مهم:** غيّر كلمات المرور فوراً بعد أول تسجيل دخول!

---

## 🔄 تحديث المشروع مستقبلاً

عند إضافة تحديثات جديدة:

```bash
# اتصل بالسيرفر
ssh master_username@server_ip

# انتقل لمجلد المشروع
cd /home/master_username/applications/app_name/public_html

# شغّل سكريبت التحديث
bash deploy-cloudways.sh
```

---

## 🆘 حل المشاكل الشائعة

### المشكلة: صفحة 404 أو 500

**الحل:**
```bash
# تأكد من Webroot
# يجب أن يكون: public_html/public

# تأكد من الصلاحيات
chmod -R 755 storage bootstrap/cache
```

### المشكلة: خطأ في قاعدة البيانات

**الحل:**
```bash
# تحقق من ملف .env
nano .env

# تأكد من صحة بيانات قاعدة البيانات
```

### المشكلة: الصفحة بيضاء فارغة

**الحل:**
```bash
# امسح الـ cache
php artisan config:clear
php artisan cache:clear
php artisan view:clear

# أعد بناء الـ cache
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### المشكلة: Assets لا تظهر

**الحل:**
```bash
# أعد بناء الـ assets
npm run build

# أنشئ symbolic link للـ storage
php artisan storage:link
```

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع ملف `CLOUDWAYS_DEPLOYMENT.md` للتفاصيل الكاملة
2. تحقق من logs في Cloudways Dashboard
3. تحقق من Laravel logs في `storage/logs/laravel.log`

---

## 🎉 تم!

مبروك! تطبيقك الآن يعمل على Cloudways! 🚀

